# 판게아 게임 — UI 화면 전이 명세

규칙은 별도로 전달됨. 이 문서는 화면 상태와 전이만 다룬다.
시작 화면(인원 선택)은 이번 범위에서 제외. 게임이 이미 시작된 상태에서 진입한다고 가정.

## 공통

- 캔버스 1920×1080 고정. 반응형 없음. 축소가 필요하면 전체 스케일링만.
- 폰트 Pretendard.
- 판게아 조각 SVG는 이미 보유한 애셋 사용.
- 상태 이름은 아래 표기를 그대로 코드 상수로 쓸 것.
- 데이터 모델(카드, 조각, 퀴즈 문항 스키마)은 구현자가 설계해서 제안할 것. 확정 전에 스키마 초안을 먼저 보여줄 것.

## 상태

### OTHERS_TURN
축소 뷰. 화면 중앙에 판 카드, 우측에 퀴즈카드·황금카드 덱, 상·좌·우에 상대 손패(분홍, 뒷면), 하단에 내 손패(파랑, 뒷면).
내 조작 없음. 상대 턴이 끝나면 자동으로 TURN_START_MODAL.

### TURN_START_MODAL
"당신 차례입니다" 모달. 확인 시 MY_TURN으로 전이.

### MY_TURN
TURN_START_MODAL에서 진입할 때 전환 연출이 있다.
- 판과 내 손패가 위로 올라오며 확대된다.
- 상대 손패는 사라진다.
- 좌우 화살표(< >) 버튼이 활성화된다.

가능한 조작: 손패 카드 탭 → SELECT_CARD.
(덱에서 뽑기 액션의 진입 UI 미정 — 아래 미결 항목 참조.)

### SELECT_CARD
선택한 카드가 판 위로 올라와 확대되고 앞면(예: "+1")이 보인다. 배경은 어둡게 딤 처리.
버튼 3개: 선택 / 취소 / 판 보기.
- 선택 → SELECT_PANGAEA
- 취소 → MY_TURN
- 판 보기 → BOARD_MODAL

### BOARD_MODAL
판 전체를 모달로 크게 띄운다. 닫으면 SELECT_CARD로 복귀. 카드 선택 상태는 유지된다.

### SELECT_PANGAEA
배경 딤. 판게아 조각들이 확대되어 표시되고 선택 가능한 조각만 채색, 나머지는 어둡게.
버튼: 선택 / 취소.
- 선택 → ON_QUIZ

### ON_QUIZ
퀴즈 모달. 상단에 문제, 2×2 배치 보기 4개, 하단에 제출 / 포기.
보기 하나를 고른 뒤 제출하면 채점하여 SUCCESS 또는 FAIL.

### SUCCESS
성공 효과음 재생. 선택한 조각이 카드 값만큼 판 위에서 이동하는 애니메이션.
연출 종료 후 다음 플레이어의 OTHERS_TURN.

### FAIL
실패 효과음 재생. 판은 움직이지 않는다.
연출 종료 후 다음 플레이어의 OTHERS_TURN.

## 미결 항목 — 임의로 정하지 말고 물어볼 것

1. SELECT_PANGAEA의 "취소"가 SELECT_CARD로 돌아가는지 MY_TURN까지 돌아가는지.
2. ON_QUIZ의 "포기"를 눌렀을 때 카드가 소모되는지, 턴이 그대로 끝나는지, 어느 상태로 돌아가는지.
3. 덱에서 카드를 뽑는 액션을 MY_TURN에서 어떻게 진입시킬지 (덱 탭? 별도 버튼?).
4. SUCCESS와 FAIL의 화면 표시 차이. 전달된 스크린샷 2장이 동일하므로 효과음·판 이동 외에 시각 표시(토스트/모달/색 플래시)가 있는지 미정.
5. 참가 인원이 2명 또는 3명일 때 상대 손패를 상·좌·우 중 어디에 배치할지.
6. 게임 종료 판정 시점과 결과 화면.
